/**
 * Session summary for list and detail responses.
 */
export interface SessionResponseDto {
  id: string;
  eventId: string;
  startsAt: string;
  venue: string;
  createdAt: string;
  updatedAt: string;
}
